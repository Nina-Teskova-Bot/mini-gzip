[package]
name = "mini-gzip"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
cargo-fuzz = "0.12.0"
flate2 = { version = "1.0", default-features = false, features = ["rust_backend"] }
proptest = "1.6"
tempfile = "3"
which = "7"
